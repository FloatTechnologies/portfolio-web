# Canonical site URL (no trailing slash). Required for sitemap, Open Graph, and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://omershafique.dev

# Optional: Google Analytics 4 measurement ID (omit to disable analytics)
# NEXT_PUBLIC_GA_ID=
